spring:
  profiles:
    include: jwt, data, liquibase
  servlet:
    multipart:
      max-file-size: 600MB
      max-request-size: 600MB

server:
  port: 9000

jwt:
  app:
    jwtSecret: WA_JWT_APP_SECRET
    jwtExpirationMs: 14_400_000 # 900_000s = 15 minutes